name: bsrnn
name_out_dir: bsrnn   # =name by default
name_tblog_dir: bsrnn # =name by default

target: vocals
sample_rate: ${sample_rate}
n_fft: ${n_fft}
n_hop: ${n_hop}

# Model parameters
feature_dim: 64
num_repeat: 8
time_layer: lstm
band_layer: lstm
n_att_head: 0
attn_enc_dim: 0